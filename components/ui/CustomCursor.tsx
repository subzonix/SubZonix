"use client";

import React from "react";

export default function CustomCursor({ children }: { children?: React.ReactNode }) {
  return (
    <>
      {children}
    </>
  ); // Placeholder that renders children if provided
}
